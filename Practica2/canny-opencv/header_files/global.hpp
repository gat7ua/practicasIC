#ifndef GLOBAL_VARS
#define GLOBAL_VARS

#include <cmath>
#include <fstream>
#include <iostream>
#include <string.h>
#include <vector>

// Variables globales: type, height, width, intensity, hi, lo, y sig
extern char type[10];
extern int height;
extern int width;
extern int intensity;
extern int hi;
extern int lo;
extern double sig;

#endif